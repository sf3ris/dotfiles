local options = {
  ensure_installed = { "lua", "rust", "toml", "elixir", "heex", "eex" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
